name: Release
# @org-managed: workflows — generated from org.toml [workflows].enabled by the org sync (ADR-004). Edit org.toml, not this file.
on:
  push:
    branches: [main, master]
permissions:
  contents: write
  pull-requests: write
jobs:
  release:
    uses: {{ORG_GITHUB}}/.github/.github/workflows/sync-release.yml@v2
    secrets: inherit
