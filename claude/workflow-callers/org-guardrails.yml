name: Guardrails
# @org-managed: workflows — generated from org.toml [workflows].enabled by the org sync (ADR-004). Edit org.toml, not this file.
on:
  pull_request:
    branches: [staging, main, master]
permissions:
  contents: read
  pull-requests: write
jobs:
  guardrails:
    uses: {{ORG_GITHUB}}/.github/.github/workflows/org-guardrails.yml@v2
    secrets: inherit
