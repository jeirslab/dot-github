name: Gate
# @org-managed: workflows — generated from org.toml [workflows].enabled by the org sync (ADR-004). Edit org.toml, not this file.
on:
  pull_request:
    branches: [staging, main, master]
  push:
    branches: [staging]
permissions:
  contents: read
jobs:
  gate:
    uses: {{ORG_GITHUB}}/.github/.github/workflows/org-gate.yml@v2
    secrets: inherit
